package comments;

//import java.util.concurrent.TimeUnit;

//import org.openqa.selenium.By;
//import org.openqa.selenium.interactions.Actions;
import org.testng.annotations.Test;

public class SamleComments {
	
	
	// driverForFireFox.close();

			// System.out.println(driverForFireFox.getClass());
	
	//ac.moveToElement(el).click().perform();
	
			// driverForFireFox.manage().timeouts().implicitlyWait(30,TimeUnit.SECONDS);
					// ac.moveToElement(driverForFireFox.findElement(By.xpath("//div[@class='files-header-action-files']/a[@id='files-header-addfolder']"))).click().build();
			//WebDriverWait wait = new WebDriverWait(driverForFireFox, 25);
			////div[@class='files-header-action-files']/a[@id='files-header-addfolder']
			
			//div[@class='files-header-action-wrapper']/div[1]/a[2]
			
			// driverForFireFox.findElement(By.xpath(".//*[@id='folder-edit-name-input']")).clear();

			//driverForFireFox.findElement(By.xpath(".//*[@id='folder-edit-name-input']")).sendKeys("NewCenturyBegain");

			//driverForFireFox.findElement(By.id("folder-edit-name-input-ok")).click();

			//driverForFireFox.navigate().refresh();
	
	
	//a.moveToElement(driverForFireFox.findElement(By.xpath("//*[@id='filesandfolders-link-members']"))).click().perform();

	// List<WebElement> ll=
	// driverForFireFox.findElements(By.xpath("//div[@id='files-item-list']/section"));

	// System.out.println(ll.size());

	// WebElement el=
	// driverForFireFox.findElement(By.xpath("//div[@id='files-item-list']/section"));

	// Select c=(Select)el;

	// c.selectByIndex(2);

	// a.moveToElement(driverForFireFox.findElement(By.xpath("//div[@class
	// ='modal-content']/button[@id='members-add-dropdown-btn']"))).click().perform();

	// div[@class ='modal-content']/button[@id='members-add-dropdown-btn']
	
	//import java.awt.Desktop.Action;
	//import java.util.List;
	//import org.openqa.selenium.Dimension;
	//import org.openqa.selenium.WebElement;
	//import org.openqa.selenium.support.ui.Select;
	//import org.openqa.selenium.support.ui.ExpectedConditions;
	//import org.openqa.selenium.support.ui.WebDriverWait;
	
	// return driverForFireFox;
	//import org.testng.annotations.Test;
	
	
	

	//driverForFireFox.manage().timeouts().implicitlyWait(20, TimeUnit.SECONDS);

	//Actions a = new Actions(driverForFireFox);

	//a.moveToElement(driverForFireFox.findElement(By.xpath("//div[@id='files-item-list']/section[2]"))).click().perform();
	
	@Test(enabled =false)
	public void permissions() {
		
	
		
		// waiting for the page loading 

	
		//JavascriptExecutor js = (JavascriptExecutor) driverForFireFox; 
		
		//js.executeScript();
		
		
		//JavascriptExecutor js = (JavascriptExecutor) driverForFireFox;
				//js.executeScript(Document.TitleProperty, toString());
		

	}

}
